# @pm/client application

## enviromental variables

Use `.env` file to configure the application or pass those variables directly.

Required variables:

- `BACKEND` - URL where backend listens

```dotenv
BACKEND=http://localhost:3000
```
